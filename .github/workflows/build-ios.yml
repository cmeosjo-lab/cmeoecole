name: Ancienne chaîne iOS - désactivée
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  migration:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Utilisez GESTCOURS V2.4 - tests et applications. Les IPA non signées nécessitent une signature Apple ou un outil de signature personnelle.'
