name: Ancienne chaîne Android - désactivée
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  migration:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Utilisez GESTCOURS V2.4 - tests et applications. Les APK doivent conserver la signature permanente GESTCOURS.'
