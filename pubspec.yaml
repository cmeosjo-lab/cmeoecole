name: ecole_gestion_prof_mobile
description: Application Prof mobile locale Android/iOS pour GESTCOURS.
publish_to: 'none'
version: 0.5.5+12

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  shared_preferences: ^2.5.5
  mobile_scanner: ^7.4.2
  uuid: ^4.6.0
  permission_handler: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
